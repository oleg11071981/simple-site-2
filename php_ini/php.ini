# Deprecated: use php.dev.ini (development) or php.prod.ini (production)
# Kept for backward compatibility with older docker-compose setups.

display_errors = On
max_execution_time = 1000
max_input_time = 1000
